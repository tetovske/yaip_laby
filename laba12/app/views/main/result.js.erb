var str = `
    <li class="responce-card">
        <% unless @res.empty? %>
            <% @res.each do |elem| %>
                <p class="res-elem"><%= elem.first %> <%= elem.second %></p>
            <% end %>
        <% else %>
            <p class="res-elem">В заданном диапазоне нет дружественных чисел!</p>
        <% end %>
    </li>
`;

$('#output-list').append(str)